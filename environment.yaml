name: dask-cluster
channels:
  - defaults
  - conda-forge
  - pytorch
  - nvidia
dependencies:
  - python=3.10
  - pip
  - pip:
    - pandas==2.2.3
    - numpy==2.1.2
    - dask[complete]==2024.9.1
    - dask-expr==1.1.15
    - s3fs
    - jupyter
  